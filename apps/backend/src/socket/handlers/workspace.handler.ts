import { Server } from 'socket.io';

export function handleWorkspace(io: Server): void {
  // TODO: listen for workspace:join, workspace:leave events
  // TODO: manage workspace rooms and broadcast membership changes
  void io;
}
