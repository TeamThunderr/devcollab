# DevCollab Quick Start

Use `README.md` for the current Docker and local development setup.

The backend now uses raw PostgreSQL through `pg`, with schema initialization handled by `apps/backend/src/db/init.ts`.
